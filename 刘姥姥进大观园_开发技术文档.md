# 刘姥姥进大观园 - 沉浸式游览项目开发技术文档

## 一、项目概述

### 1.1 项目名称
刘姥姥进大观园 - 3D沉浸式虚拟游览体验

### 1.2 项目背景
基于《红楼梦》经典情节"刘姥姥进大观园"，打造一款第一人称3D沉浸式游览体验。玩家将扮演刘姥姥，以乡下老妪的视角游览大观园，体验古代园林之美，触发各种剧情事件。

### 1.3 核心体验
- **视角代入**：以刘姥姥的第一人称视角，感受大观园的宏伟与精致
- **文化沉浸**：融入红楼梦元素，诗词、建筑、服饰、饮食全方位呈现
- **剧情触发**：游览过程中触发经典场景（如潇湘馆、怡红院、栊翠庵等）
- **互动探索**：与NPC对话、观察景物、收集物品

### 1.4 技术栈
| 组件 | 技术选型 | 版本 |
|------|----------|------|
| 游戏引擎 | Godot | 4.7 |
| 渲染管线 | Forward Plus | - |
| 物理引擎 | Jolt Physics | - |
| 渲染API | Direct3D 12 | Windows |
| 脚本语言 | GDScript | - |
| MCP集成 | godot_ai | 最新版 |

---

## 二、系统架构设计

### 2.1 场景层级结构

```
Main (Node3D) - 主场景根节点
├── WorldEnvironment - 世界环境（天空、雾效、色调映射）
├── DirectionalLight3D (Sun) - 太阳光源
├── Player (CharacterBody3D) - 玩家角色
│   ├── CollisionShape3D - 碰撞体
│   ├── Camera3D - 第一人称摄像机
│   ├── RayCast3D - 交互射线
│   ├── InteractionArea (Area3D) - 交互区域检测
│   └── UI (CanvasLayer) - 玩家UI层
│       ├── Crosshair (CenterContainer) - 准星
│       ├── DialogBox (PanelContainer) - 对话框
│       ├── PromptLabel (Label) - 交互提示
│       └── InventoryPanel (PanelContainer) - 物品栏
├── Terrain (Node3D) - 地形系统
│   ├── Ground (MeshInstance3D) - 地面
│   ├── WaterSystem (Node3D) - 水系
│   └── Paths (Node3D) - 道路系统
├── Buildings (Node3D) - 建筑群
│   ├── XiaoxiangGuan (潇湘馆)
│   ├── YihongYuan (怡红院)
│   ├── LongcuiAn (栊翠庵)
│   ├── DaguanyuanGate (大观园正门)
│   └── ... (其他建筑)
├── Vegetation (Node3D) - 植被系统
│   ├── Trees (Node3D) - 树木
│   ├── Flowers (Node3D) - 花卉
│   └── Bamboo (Node3D) - 竹林
├── NPCs (Node3D) - NPC管理
│   ├── GrandmaLiu (刘姥姥-玩家)
│   ├── JiaMu (贾母)
│   ├── WangXifeng (王熙凤)
│   ├── LinDaiyu (林黛玉)
│   └── ... (其他NPC)
├── Items (Node3D) - 可交互物品
├── TriggerZones (Node3D) - 触发区域
└── Systems (Node3D) - 全局系统
    ├── AudioSystem - 音频系统
    ├── SaveSystem - 存档系统
    └── EventSystem - 事件系统
```

### 2.2 核心系统架构

```
┌─────────────────────────────────────────────────────────┐
│                    GameManager (Autoload)                │
│  - 场景切换管理、全局状态、游戏流程控制                        │
└──────────────────────┬──────────────────────────────────┘
                       │
       ┌───────────────┼───────────────┐
       ▼               ▼               ▼
┌──────────┐    ┌──────────┐    ┌──────────┐
│ Player   │    │ Dialog   │    │ Quest    │
│ System   │    │ System   │    │ System   │
└──────────┘    └──────────┘    └──────────┘
       │               │               │
       ▼               ▼               ▼
┌──────────┐    ┌──────────┐    ┌──────────┐
│ Input    │    │ NPC      │    │ Event    │
│ Manager  │    │ Manager  │    │ Trigger  │
└──────────┘    └──────────┘    └──────────┘
       │               │               │
       └───────────────┼───────────────┘
                       ▼
              ┌─────────────────┐
              │  World/Scene    │
              │  (3D场景资源)    │
              └─────────────────┘
```

---

## 三、模块详细设计

### 3.1 玩家系统 (PlayerSystem)

#### 3.1.1 移动控制
```gdscript
# player.gd - 玩家控制器
extends CharacterBody3D

# 移动参数
@export var walk_speed: float = 3.0
@export var run_speed: float = 5.5
@export var jump_velocity: float = 4.5
@export var mouse_sensitivity: float = 0.002
@export var look_limit: float = 80.0

# 重力
var gravity = ProjectSettings.get_setting("physics/3d/default_gravity")

# 节点引用
@onready var camera: Camera3D = $Camera3D
@onready var raycast: RayCast3D = $Camera3D/RayCast3D
@onready var interaction_area: Area3D = $InteractionArea

# 状态
var is_running: bool = false
var can_interact: bool = false
var current_interactable: Node = null

func _ready():
    Input.set_mouse_mode(Input.MOUSE_MODE_CAPTURED)

func _input(event):
    # 鼠标视角控制
    if event is InputEventMouseMotion:
        rotate_y(-event.relative.x * mouse_sensitivity)
        camera.rotate_x(-event.relative.y * mouse_sensitivity)
        camera.rotation.x = clamp(
            camera.rotation.x,
            deg_to_rad(-look_limit),
            deg_to_rad(look_limit)
        )
    
    # ESC键释放鼠标
    if event.is_action_pressed("ui_cancel"):
        Input.set_mouse_mode(Input.MOUSE_MODE_VISIBLE)

func _physics_process(delta):
    # 重力
    if not is_on_floor():
        velocity.y -= gravity * delta
    
    # 跳跃
    if Input.is_action_just_pressed("jump") and is_on_floor():
        velocity.y = jump_velocity
    
    # 移动方向
    var input_dir = Input.get_vector("move_left", "move_right", "move_forward", "move_backward")
    var direction = (transform.basis * Vector3(input_dir.x, 0, input_dir.y)).normalized()
    
    # 速度
    is_running = Input.is_action_pressed("sprint")
    var current_speed = run_speed if is_running else walk_speed
    
    if direction:
        velocity.x = direction.x * current_speed
        velocity.z = direction.z * current_speed
    else:
        velocity.x = move_toward(velocity.x, 0, current_speed)
        velocity.z = move_toward(velocity.z, 0, current_speed)
    
    move_and_slide()
    
    # 交互检测
    check_interaction()

func check_interaction():
    if raycast.is_colliding():
        var collider = raycast.get_collider()
        if collider.has_method("get_interaction_info"):
            current_interactable = collider
            can_interact = true
            show_interaction_prompt(collider.get_interaction_info())
        else:
            current_interactable = null
            can_interact = false
            hide_interaction_prompt()
    
    if can_interact and Input.is_action_just_pressed("interact"):
        current_interactable.interact(self)
```

#### 3.1.2 输入映射配置
```
# 需要在项目设置中配置的输入动作：
move_forward  - W键
move_backward - S键
move_left     - A键
move_right    - D键
jump          - 空格键
sprint        - Shift键
interact      - E键
inventory     - I键
dialog_next   - 鼠标左键/空格
pause_menu    - ESC键
```

### 3.2 对话系统 (DialogSystem)

#### 3.2.1 对话数据结构
```gdscript
# dialog_data.gd - 对话数据定义
class_name DialogData

# 对话节点结构
static var dialogs = {
    "jiamu_greeting": {
        "speaker": "贾母",
        "portrait": "res://assets/portraits/jiamu.png",
        "text": "这位便是刘姥姥吧？快请坐。",
        "choices": [
            {
                "text": "（拘谨地行礼）给老太太请安了。",
                "next": "jiamu_greeting_2"
            },
            {
                "text": "（四处张望）哎呦，这屋子真气派！",
                "next": "jiamu_surprised"
            }
        ],
        "conditions": {},
        "events": ["unlock_jiamu_topic"]
    },
    "jiamu_greeting_2": {
        "speaker": "刘姥姥",
        "text": "给老太太请安了。我们乡下人，没见过世面，还望老太太莫怪。",
        "next": "jiamu_kind"
    },
    # ... 更多对话节点
}
```

#### 3.2.2 对话管理器
```gdscript
# dialog_manager.gd
extends Node
class_name DialogManager

signal dialog_started(dialog_id: String)
signal dialog_ended(dialog_id: String)
signal choice_made(dialog_id: String, choice_index: int)

var current_dialog: Dictionary = {}
var is_dialog_active: bool = false
var dialog_history: Array = []

@onready var dialog_ui: Control = $DialogUI

func start_dialog(dialog_id: String):
    if DialogData.dialogs.has(dialog_id):
        current_dialog = DialogData.dialogs[dialog_id]
        is_dialog_active = true
        dialog_history.append(dialog_id)
        
        dialog_ui.show_dialog(
            current_dialog.speaker,
            current_dialog.text,
            current_dialog.get("portrait", ""),
            current_dialog.get("choices", [])
        )
        
        dialog_started.emit(dialog_id)

func select_choice(choice_index: int):
    if current_dialog.has("choices") and choice_index < current_dialog.choices.size():
        var choice = current_dialog.choices[choice_index]
        choice_made.emit(dialog_history.back(), choice_index)
        
        # 执行关联事件
        if choice.has("events"):
            for event in choice.events:
                EventSystem.trigger_event(event)
        
        # 跳转下一个对话
        if choice.has("next"):
            start_dialog(choice.next)
        else:
            end_dialog()

func end_dialog():
    is_dialog_active = false
    dialog_ui.hide_dialog()
    dialog_ended.emit(dialog_history.back())
```

### 3.3 NPC系统 (NPCSystem)

#### 3.3.1 NPC基类
```gdscript
# npc_base.gd
extends CharacterBody3D
class_name NPCBase

@export var npc_name: String = ""
@export var npc_title: String = ""
@export var dialog_id: String = ""
@export var patrol_points: Array[Marker3D] = []
@export var idle_animations: Array[String] = []

@onready var nav_agent: NavigationAgent3D = $NavigationAgent3D
@onready var anim_player: AnimationPlayer = $AnimationPlayer
@onready var name_label: Label3D = $NameLabel

var current_patrol_index: int = 0
var is_moving: bool = false
var can_interact: bool = false

enum NPCState { IDLE, PATROL, TALKING, FOLLOWING }
var current_state: NPCState = NPCState.IDLE

func _ready():
    name_label.text = npc_name
    if patrol_points.size() > 0:
        current_state = NPCState.PATROL

func _physics_process(delta):
    match current_state:
        NPCState.IDLE:
            play_idle_animation()
        NPCState.PATROL:
            patrol(delta)
        NPCState.TALKING:
            look_at_player()

func patrol(delta):
    if patrol_points.size() == 0:
        return
    
    var target = patrol_points[current_patrol_index].global_position
    nav_agent.target_position = target
    
    if nav_agent.is_navigation_finished():
        current_patrol_index = (current_patrol_index + 1) % patrol_points.size()
        return
    
    var next_pos = nav_agent.get_next_path_position()
    var direction = (next_pos - global_position).normalized()
    
    velocity = direction * 2.0
    move_and_slide()
    
    # 面向移动方向
    if velocity.length() > 0.1:
        var look_target = global_position + velocity.normalized()
        look_at(look_target, Vector3.UP)

func interact(player):
    current_state = NPCState.TALKING
    DialogManager.start_dialog(dialog_id)

func get_interaction_info() -> Dictionary:
    return {
        "name": npc_name,
        "action": "对话",
        "key": "E"
    }
```

### 3.4 建筑场景模块

#### 3.4.1 大观园主要建筑列表

| 建筑名称 | 场景文件 | 功能描述 | 关键剧情 |
|---------|---------|---------|---------|
| 大观园正门 | entrance.tscn | 入口区域 | 刘姥姥初见大观园 |
| 潇湘馆 | xiaoxiang_guan.tscn | 林黛玉居所 | 竹林、诗社 |
| 怡红院 | yihong_yuan.tscn | 贾宝玉居所 | 贵公子生活 |
| 栊翠庵 | longcui_an.tscn | 妙玉修行处 | 品茶情节 |
| 稻香村 | daoxiang_cun.tscn | 李纨居所 | 田园风光 |
| 蘅芜苑 | hengwu_yuan.tscn | 薛宝钗居所 | 花卉园林 |
| 省亲别墅 | shengqin_bieshu.tscn | 元妃省亲处 | 宏伟建筑 |
| 大观楼 | daguan_lou.tscn | 主楼 | 宴会场景 |
| 藕香榭 | ouxiang_xie.tscn | 水榭 | 赏荷 |
| 暖香坞 | nuanxiang_wu.tscn | 惜春居所 | 作画 |

#### 3.4.2 建筑场景规范
```gdscript
# building_base.gd - 建筑基类
extends Node3D
class_name BuildingBase

@export var building_name: String = ""
@export var building_description: String = ""
@export var associated_character: String = ""
@export var unlock_condition: String = ""

@onready var entrance_area: Area3D = $EntranceArea
@onready var name_plate: Label3D = $NamePlate

func _ready():
    name_plate.text = building_name
    entrance_area.body_entered.connect(_on_player_entered)

func _on_player_entered(body):
    if body is Player:
        EventBus.building_entered.emit(building_name)
        show_building_info()

func show_building_info():
    # 显示建筑介绍UI
    pass
```

### 3.5 环境与氛围系统

#### 3.5.1 时间与天气系统
```gdscript
# time_weather_system.gd
extends Node

signal time_changed(hour: int, minute: int)
signal weather_changed(weather_type: String)

@export var day_duration_minutes: float = 20.0  # 游戏内一天=现实20分钟
@export var start_hour: int = 8
@export var start_minute: int = 0

var current_hour: int = 8
var current_minute: int = 0
var time_scale: float = 1.0

# 天空预设
var sky_presets = {
    "morning": {
        "sun_color": Color(1.0, 0.95, 0.8),
        "sun_energy": 0.8,
        "fog_color": Color(0.9, 0.85, 0.75),
        "ambient_color": Color(0.4, 0.35, 0.3)
    },
    "noon": {
        "sun_color": Color(1.0, 0.98, 0.95),
        "sun_energy": 1.2,
        "fog_color": Color(0.85, 0.88, 0.9),
        "ambient_color": Color(0.5, 0.5, 0.5)
    },
    "evening": {
        "sun_color": Color(1.0, 0.7, 0.4),
        "sun_energy": 0.6,
        "fog_color": Color(0.9, 0.6, 0.4),
        "ambient_color": Color(0.4, 0.25, 0.15)
    },
    "night": {
        "sun_color": Color(0.2, 0.2, 0.4),
        "sun_energy": 0.1,
        "fog_color": Color(0.1, 0.1, 0.2),
        "ambient_color": Color(0.1, 0.1, 0.15)
    }
}

@onready var sun: DirectionalLight3D = $"../Sun"
@onready var environment: WorldEnvironment = $"../WorldEnvironment"

func _process(delta):
    update_time(delta)
    update_environment()

func update_time(delta):
    var minutes_per_second = (24.0 * 60.0) / (day_duration_minutes * 60.0)
    current_minute += minutes_per_second * delta * time_scale
    
    if current_minute >= 60:
        current_minute -= 60
        current_hour += 1
        
        if current_hour >= 24:
            current_hour = 0
        
        time_changed.emit(current_hour, current_minute)

func update_environment():
    var time_factor = current_hour + current_minute / 60.0
    var preset = get_time_preset(time_factor)
    
    sun.light_color = preset.sun_color
    sun.light_energy = preset.sun_energy
    environment.environment.ambient_light_color = preset.ambient_color

func get_time_preset(hour: float) -> Dictionary:
    if hour < 6:
        return sky_presets.night
    elif hour < 9:
        return lerp_preset(sky_presets.night, sky_presets.morning, (hour - 6) / 3.0)
    elif hour < 12:
        return lerp_preset(sky_presets.morning, sky_presets.noon, (hour - 9) / 3.0)
    elif hour < 17:
        return sky_presets.noon
    elif hour < 20:
        return lerp_preset(sky_presets.noon, sky_presets.evening, (hour - 17) / 3.0)
    else:
        return lerp_preset(sky_presets.evening, sky_presets.night, (hour - 20) / 4.0)
```

#### 3.5.2 音效系统
```gdscript
# audio_system.gd
extends Node

# 环境音效
@onready var ambient_wind: AudioStreamPlayer = $AmbientWind
@onready var ambient_birds: AudioStreamPlayer = $AmbientBirds
@onready var ambient_water: AudioStreamPlayer = $AmbientWater
@onready var ambient_bamboo: AudioStreamPlayer = $AmbientBamboo

# 背景音乐
@onready var bgm_player: AudioStreamPlayer = $BGMPlayer

# 音效池
var sfx_pool: Array[AudioStreamPlayer] = []

# 场景音效配置
var scene_audio_config = {
    "entrance": {
        "bgm": "res://assets/audio/bgm/ancient_theme.ogg",
        "ambient": ["birds", "wind"]
    },
    "xiaoxiang_guan": {
        "bgm": "res://assets/audio/bgm/serene_theme.ogg",
        "ambient": ["bamboo", "birds"]
    },
    "water_area": {
        "bgm": "res://assets/audio/bgm/water_theme.ogg",
        "ambient": ["water", "birds"]
    }
}

func switch_scene_audio(scene_name: String):
    if scene_audio_config.has(scene_name):
        var config = scene_audio_config[scene_name]
        
        # 切换BGM
        if config.has("bgm"):
            crossfade_bgm(config.bgm)
        
        # 切换环境音
        stop_all_ambient()
        for ambient_name in config.ambient:
            play_ambient(ambient_name)

func crossfade_bgm(new_track_path: String, fade_time: float = 1.0):
    var tween = create_tween()
    tween.tween_property(bgm_player, "volume_db", -40.0, fade_time)
    await tween.finished
    
    bgm_player.stream = load(new_track_path)
    bgm_player.play()
    
    var tween2 = create_tween()
    tween2.tween_property(bgm_player, "volume_db", 0.0, fade_time)

func play_ambient(name: String):
    match name:
        "wind":
            ambient_wind.play()
        "birds":
            ambient_birds.play()
        "water":
            ambient_water.play()
        "bamboo":
            ambient_bamboo.play()
```

### 3.6 事件与剧情系统

#### 3.6.1 事件触发器
```gdscript
# event_trigger.gd
extends Area3D
class_name EventTrigger

@export var event_id: String = ""
@export var trigger_once: bool = true
@export var trigger_on_enter: bool = true
@export var required_conditions: Dictionary = {}

var has_triggered: bool = false

func _ready():
    body_entered.connect(_on_body_entered)
    body_exited.connect(_on_body_exited)

func _on_body_entered(body):
    if body is Player and not (trigger_once and has_triggered):
        if check_conditions():
            trigger_event()

func trigger_event():
    has_triggered = true
    EventBus.trigger_event(event_id)

func check_conditions() -> bool:
    for condition_key in required_conditions:
        if not GameState.check_condition(condition_key, required_conditions[condition_key]):
            return false
    return true
```

#### 3.6.2 剧情事件列表

| 事件ID | 触发位置 | 剧情描述 | 奖励/解锁 |
|--------|---------|---------|----------|
| intro_arrival | 大观园门口 | 刘姥姥到达大观园 | 开始游览 |
| meet_jiamu | 荣庆堂 | 拜见贾母 | 解锁更多区域 |
| visit_xiaoxiang | 潇湘馆 | 见到林黛玉 | 获得诗集 |
| visit_yihong | 怡红院 | 见到贾宝玉 | 了解宝玉性格 |
| tea_ceremony | 栊翠庵 | 妙玉奉茶 | 获得茶杯 |
| banquet | 大观楼 | 参加宴会 | 经典场景重现 |
| garden_walk | 各处花园 | 游览花园 | 收集花卉 |
| painting | 暖香坞 | 看惜春作画 | 获得画作 |
| farewell | 大观园门口 | 离开大观园 | 游戏结束 |

### 3.7 存档系统

```gdscript
# save_system.gd
extends Node

const SAVE_PATH = "user://saves/"
const SAVE_FILE = "save_%d.json"

func save_game(slot: int = 0):
    var save_data = {
        "version": "1.0",
        "timestamp": Time.get_datetime_string_from_system(),
        "player": {
            "position": Player.global_position,
            "rotation": Player.global_rotation
        },
        "game_state": GameState.serialize(),
        "dialog_history": DialogManager.dialog_history,
        "collected_items": InventorySystem.get_items(),
        "unlocked_areas": AreaManager.get_unlocked_areas(),
        "completed_events": EventSystem.get_completed_events()
    }
    
    DirAccess.make_dir_recursive_absolute(SAVE_PATH)
    var file = FileAccess.open(SAVE_PATH + SAVE_FILE % slot, FileAccess.WRITE)
    file.store_string(JSON.stringify(save_data, "\t"))
    file.close()

func load_game(slot: int = 0) -> bool:
    var file_path = SAVE_PATH + SAVE_FILE % slot
    
    if not FileAccess.file_exists(file_path):
        return false
    
    var file = FileAccess.open(file_path, FileAccess.READ)
    var json = JSON.new()
    var error = json.parse(file.get_as_text())
    file.close()
    
    if error == OK:
        var save_data = json.data
        apply_save_data(save_data)
        return true
    
    return false

func apply_save_data(data: Dictionary):
    # 恢复玩家位置
    Player.global_position = data.player.position
    Player.global_rotation = data.player.rotation
    
    # 恢复游戏状态
    GameState.deserialize(data.game_state)
    
    # 恢复其他系统状态
    DialogManager.dialog_history = data.dialog_history
    InventorySystem.load_items(data.collected_items)
    AreaManager.load_unlocked_areas(data.unlocked_areas)
    EventSystem.load_completed_events(data.completed_events)
```

---

## 四、美术资源规范

### 4.1 模型规格

| 资源类型 | 面数上限 | 贴图尺寸 | 格式 |
|---------|---------|---------|------|
| 角色模型 | 15,000面 | 2048x2048 | glTF 2.0 |
| 建筑主体 | 30,000面 | 2048x2048 | glTF 2.0 |
| 场景道具 | 5,000面 | 1024x1024 | glTF 2.0 |
| 植被 | 500面 | 512x512 | glTF 2.0 |

### 4.2 材质规范
```
# 标准材质配置
StandardMaterial3D:
  - albedo_color: 基础颜色
  - metallic: 0.0 (建筑) / 0.2 (金属件)
  - roughness: 0.7 (建筑) / 0.3 (光滑表面)
  - normal_enabled: true
  - ao_enabled: true
  
# 特殊材质
- 水面: 使用Shader实现波纹反射
- 竹子: 使用Alpha Scissor处理叶片
- 纱帘: 使用半透明材质
```

### 4.3 动画规格
| 动画类型 | 帧率 | 时长 | 格式 |
|---------|------|------|------|
| 行走 | 30fps | 1秒循环 | glTF |
| 待机 | 30fps | 3秒循环 | glTF |
| 交互 | 30fps | 2-3秒 | glTF |
| 表情 | 30fps | 1-2秒 | Shape Keys |

---

## 五、音频资源规范

### 5.1 音频格式
| 类型 | 格式 | 采样率 | 比特率 |
|------|------|--------|--------|
| BGM | OGG Vorbis | 44100Hz | 128kbps |
| 环境音 | OGG Vorbis | 44100Hz | 96kbps |
| 音效 | WAV | 44100Hz | 16bit |
| 语音 | OGG Vorbis | 22050Hz | 64kbps |

### 5.2 音乐设计
- **主题曲**：古筝、琵琶、笛子为主，营造古典氛围
- **场景音乐**：
  - 大观园入口：庄重典雅
  - 潇湘馆：清幽淡雅
  - 怡红院：富贵华丽
  - 花园：轻快活泼
  - 宴会：喜庆热闹

---

## 六、UI设计规范

### 6.1 主要UI界面

```
MainMenu (主菜单)
├── TitleLabel - 标题
├── NewGameButton - 新游戏
├── ContinueButton - 继续
├── LoadGameButton - 读取存档
├── SettingsButton - 设置
└── QuitButton - 退出

GameHUD (游戏内HUD)
├── Crosshair - 准星
├── InteractionPrompt - 交互提示
├── LocationLabel - 位置显示
├── TimeDisplay - 时间显示
└── MiniMap - 小地图（可选）

DialogUI (对话界面)
├── SpeakerLabel - 说话人
├── PortraitTexture - 头像
├── DialogText - 对话文本
└── ChoiceContainer - 选项容器

InventoryUI (物品栏)
├── ItemGrid - 物品网格
├── ItemDetail - 物品详情
└── CloseButton - 关闭按钮
```

### 6.2 UI风格
- **配色**：中国传统色彩（朱红、藏青、米白、黛绿）
- **字体**：使用书法体或宋体
- **边框**：中式花纹边框
- **动画**：淡入淡出、滑入滑出

---

## 七、项目目录结构

```
project_godot/
├── project.godot                 # 项目配置
├── main.tscn                     # 主场景
├── main.gd                       # 主脚本
│
├── scenes/                       # 场景文件
│   ├── player/
│   │   └── player.tscn
│   ├── npcs/
│   │   ├── jiamu.tscn
│   │   ├── lindaiyu.tscn
│   │   └── ...
│   ├── buildings/
│   │   ├── entrance.tscn
│   │   ├── xiaoxiang_guan.tscn
│   │   └── ...
│   ├── environment/
│   │   ├── terrain.tscn
│   │   ├── trees.tscn
│   │   └── water.tscn
│   └── ui/
│       ├── main_menu.tscn
│       ├── game_hud.tscn
│       └── dialog_ui.tscn
│
├── scripts/                      # 脚本文件
│   ├── player/
│   │   ├── player.gd
│   │   └── player_camera.gd
│   ├── npcs/
│   │   ├── npc_base.gd
│   │   └── npc_data.gd
│   ├── systems/
│   │   ├── dialog_system.gd
│   │   ├── event_system.gd
│   │   ├── save_system.gd
│   │   ├── audio_system.gd
│   │   └── time_system.gd
│   ├── ui/
│   │   ├── main_menu.gd
│   │   ├── dialog_ui.gd
│   │   └── inventory_ui.gd
│   └── autoload/
│       ├── game_manager.gd
│       ├── game_state.gd
│       └── event_bus.gd
│
├── assets/                       # 资源文件
│   ├── models/                   # 3D模型
│   │   ├── characters/
│   │   ├── buildings/
│   │   ├── props/
│   │   └── vegetation/
│   ├── textures/                 # 纹理贴图
│   │   ├── characters/
│   │   ├── buildings/
│   │   └── ui/
│   ├── materials/                # 材质
│   ├── animations/               # 动画
│   ├── audio/                    # 音频
│   │   ├── bgm/
│   │   ├── sfx/
│   │   ├── ambient/
│   │   └── voice/
│   ├── fonts/                    # 字体
│   └── shaders/                  # 着色器
│
└── addons/                       # 插件
    └── godot_ai/                 # MCP AI插件
```

---

## 八、开发计划

### 8.1 里程碑规划

| 阶段 | 周期 | 目标 | 交付物 |
|------|------|------|--------|
| **第一阶段：原型验证** | 2周 | 基础移动、交互、一个完整场景 | 可运行Demo |
| **第二阶段：核心系统** | 3周 | 对话系统、NPC系统、存档 | 核心玩法验证 |
| **第三阶段：场景制作** | 4周 | 完成5个主要场景 | 场景游览体验 |
| **第四阶段：剧情填充** | 3周 | 完成所有剧情事件 | 完整剧情流程 |
| **第五阶段：打磨优化** | 2周 | 美术优化、音效集成、Bug修复 | 发布版本 |

### 8.2 详细任务分解

#### 第一阶段：原型验证 (第1-2周)
- [ ] 搭建基础场景框架
- [ ] 实现玩家第一人称移动
- [ ] 实现视角控制（鼠标）
- [ ] 创建基础交互系统
- [ ] 制作一个测试建筑场景
- [ ] 实现基础UI（准星、交互提示）

#### 第二阶段：核心系统 (第3-5周)
- [ ] 完成对话系统开发
- [ ] 实现NPC基础AI（巡逻、交互）
- [ ] 开发存档/读档功能
- [ ] 实现事件触发系统
- [ ] 创建物品收集系统
- [ ] 开发设置界面

#### 第三阶段：场景制作 (第6-9周)
- [ ] 制作大观园正门场景
- [ ] 制作潇湘馆场景
- [ ] 制作怡红院场景
- [ ] 制作栊翠庵场景
- [ ] 制作花园/水系场景
- [ ] 实现场景切换系统

#### 第四阶段：剧情填充 (第10-12周)
- [ ] 编写所有对话文本
- [ ] 实现剧情事件流程
- [ ] 添加NPC动画和语音
- [ ] 制作剧情过场
- [ ] 实现结局系统

#### 第五阶段：打磨优化 (第13-14周)
- [ ] 性能优化（LOD、遮挡剔除）
- [ ] 美术细节完善
- [ ] 音效集成测试
- [ ] Bug修复和测试
- [ ] 打包发布

---

## 九、性能优化策略

### 9.1 渲染优化
- **LOD系统**：远处建筑使用低面数模型
- **遮挡剔除**：使用OcclusionCuller
- **实例化渲染**：植被、重复道具使用MultiMeshInstance3D
- **纹理流式加载**：大纹理按需加载

### 9.2 场景加载
- **分块加载**：大场景分区域异步加载
- **预加载**：关键资源提前加载
- **资源缓存**：常用资源保持在内存

### 9.3 物理优化
- **简化碰撞体**：使用简单形状代替复杂网格
- **休眠机制**：静止物体自动休眠
- **分层碰撞**：合理设置碰撞层

---

## 十、MCP集成说明

### 10.1 MCP功能应用
本项目已集成`godot_ai` MCP插件，可用于：

- **场景快速生成**：通过MCP命令快速创建场景结构
- **节点批量操作**：批量创建、修改节点属性
- **动画快速配置**：使用动画预设快速创建动画
- **材质快速应用**：应用预设材质和着色器

### 10.2 MCP使用示例
```gdscript
# 通过MCP创建建筑结构
mcp_godot_ai_node_create(parent_path="Buildings", type="Node3D", name="XiaoxiangGuan")

# 设置建筑属性
mcp_godot_ai_node_set_property(path="Buildings/XiaoxiangGuan", property="position", value=Vector3(50, 0, 30))

# 批量创建植被
mcp_godot_ai_batch_execute(commands=[
    {"op": "node_create", "params": {"parent": "Vegetation/Trees", "type": "MeshInstance3D", "name": "Bamboo_1"}},
    {"op": "node_set_property", "params": {"path": "Vegetation/Trees/Bamboo_1", "property": "position", "value": Vector3(10, 0, 5)}}
])
```

---

## 十一、技术注意事项

### 11.1 已知技术限制
1. **GDScript性能**：复杂计算考虑使用C#或GDExtension
2. **Web导出**：当前配置使用D3D12，Web导出需要切换渲染器
3. **移动端适配**：需要额外的触控输入系统

### 11.2 调试建议
- 使用Godot内置调试器
- 开启可见碰撞体显示
- 使用性能监视器监控帧率
- MCP日志查看器用于AI功能调试

### 11.3 版本控制
```
# 推荐.gitignore
.import/
*.import
export.cfg
export_presets.cfg

# MCP插件可选择性提交
# addons/godot_ai/
```

---

## 十二、参考资料

### 12.1 官方文档
- [Godot 4.7 官方文档](https://docs.godotengine.org/)
- [GDScript 语法参考](https://docs.godotengine.org/en/stable/tutorials/scripting/gdscript/)
- [3D 场景构建](https://docs.godotengine.org/en/stable/tutorials/3d/index.html)

### 12.2 红楼梦参考资料
- 《红楼梦》原著第39-42回（刘姥姥相关章节）
- 大观园建筑布局考证
- 清代园林建筑风格研究

### 12.3 美术资源来源
- [Kenney.nl](https://kenney.nl/) - 免费游戏资源
- [Poly Haven](https://polyhaven.com/) - 免费3D模型和材质
- [ambientCG](https://ambientcg.com/) - 免费PBR材质

---

## 附录A：输入映射表

| 动作名称 | 按键 | 说明 |
|---------|------|------|
| move_forward | W | 前进 |
| move_backward | S | 后退 |
| move_left | A | 左移 |
| move_right | D | 右移 |
| jump | Space | 跳跃 |
| sprint | Shift | 奔跑 |
| interact | E | 交互 |
| inventory | I | 物品栏 |
| dialog_next | 鼠标左键 | 下一句对话 |
| pause | ESC | 暂停菜单 |

---

## 附录B：色彩参考

```gdscript
# 中国传统色彩
const ZHU_RED = Color(0.89, 0.22, 0.2)      # 朱红
const CANG_QING = Color(0.1, 0.2, 0.35)     # 藏青
const MI_BAI = Color(0.95, 0.92, 0.85)      # 米白
const DAI_LV = Color(0.2, 0.35, 0.2)        # 黛绿
const HUANG_QI = Color(0.95, 0.85, 0.6)     # 黄芪
const QING_HUA = Color(0.2, 0.3, 0.6)       # 青花
```

---

**文档版本**：v1.0  
**最后更新**：2026年6月22日  
**项目状态**：规划阶段
